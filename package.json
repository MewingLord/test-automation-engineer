{
   "devDependencies": {
      "chance": "^1.1.12",
      "concurrently": "^8.2.2",
      "cypress-mochawesome-reporter": "^3.8.2",
      "mochawesome": "^7.1.3",
      "mochawesome-merge": "^4.3.0",
      "mochawesome-report-generator": "^6.2.0",
      "start-server-and-test": "^2.0.5"
   },
   "scripts": {
      "start:all": "concurrently \"npm:frontend\" \"npm:backend\"",
      "frontend": "cd frontend && npm install && npm run start --host",
      "backend": "cd backend && npm install && npm run start:dev"
   },
   "dependencies": {
      "cypress": "^13.13.3"
   }
}
